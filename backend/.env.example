# Backend-only provider configuration. Never expose these values to the frontend.
OPENAI_API_KEY=
CIRCE_OPENAI_MODEL=

# Local defaults preserve execution outside Docker.
CIRCE_DATABASE_URL=sqlite:///./circe_home.db
CIRCE_MQTT_HOST=localhost
CIRCE_MQTT_PORT=1883
